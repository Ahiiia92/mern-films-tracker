<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><% if(editing){ %>Edit Film<% } else { %>Add Film<% } %></title>
</head>

<body>
<h1 className="film-title"><% if(editing){ %>Edit Film<% } else { %>Add Film<% } %></h1>

  <form method="POST" action="/<% if(editing){ %>edit-film<% } else { %>add-film<% } %>">
    <div>
      <label>Name</label>
      <input type="text" name="name" value="<% if(editing){ %><%= film.name %><% } %>" required />
    </div>
    <div>
      <label>Image</label>
      <input type="text" name="image" value="<% if(editing){ %><%= film.image %><% } %>" required />
    </div>
    <div>
      <label>Description</label>
      <input type="text" name="description" value="<% if(editing){ %><%= film.description %><% } %>" required />
    </div>
    <% if(editing) { %>
      <div>
        <input type="hidden" name="filmId" value="<%= film.id %>">
      </div>
    <% } %>
    <div>
      <button type="submit"><% if(editing) { %>Edit Film<% } else { %>Add Film<% } %></button>
    </div>
  </form>
</body>




</html>
